<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버스 공공데이터 + Google Calendar API.
         지금은 google_sign_in_android 6.2.1이 병합으로 넣어주지만, 그 플러그인이 빠지면
         조용히 사라지고 버스 카드는 "버스 정보를 불러올 수 없어요"만 띄운다. 그래서 명시한다. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 재부팅 후 예약 알림 복원. **플러그인은 이 권한을 자체 선언하지 않는다** —
         없으면 부팅 한 번에 예약 알림이 전멸한다. iOS엔 없는 문제라 놓치기 쉽다.
         (`POST_NOTIFICATIONS`는 플러그인 매니페스트가 이미 병합하므로 여기 적지 않는다.) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 시스템 캘린더 이벤트 저장(device_calendar 패키지) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- 기기를 바꿔도 1년치 일정이 복원된다. 교사에게 실질 가치가 크고, iOS의 iCloud
         백업이 이미 같은 성질이라 방침 한 문단이 양쪽을 덮는다.
         ⚠️ 기본값에 맡기지 않고 명시한다. "정하지 않아 기본값으로 출시된 것"과
            "정해서 켠 것"은 다르고, 이 한 줄이 §M3-H3 데이터 안전 답안의 전제다.
            뒤집으면 그 양식도 함께 뒤집어야 한다. -->
    <application
        android:label="공직플랜"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- CSV 열기 — 사용자가 파일을 **명시적으로 골라** 보내는 경로라 넓게 받는다.
                 `content://` URI에는 확장자가 없어 경로 패턴으로는 못 걸러지고 mimeType만
                 남는데, 파일앱·다운로드·메일 첨부가 CSV를 `text/plain`이나
                 `application/octet-stream`으로 주는 경우가 흔하다.
                 ⚠️ 대가: CSV가 아닌 파일로도 열기 목록에 뜬다. 그때는 Dart가 조용히 버린다. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/csv"/>
                <data android:mimeType="text/comma-separated-values"/>
                <data android:mimeType="application/csv"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="*/*"/>
            </intent-filter>

            <!-- CSV 공유 — 공유 목록은 **좁게** 유지한다. 여기 `*/*`나 `text/plain`을
                 넣으면 메모·사진을 공유할 때도 공직플랜이 뜨고, CSV가 아니면 Dart가
                 버리므로 사용자에겐 "눌렀는데 아무 일도 없네"가 된다.
                 대가: CSV를 `text/plain`으로 보내는 앱의 '공유'에서는 안 뜬다
                 (그 경우 사용자는 '열기'로 돌아가면 된다). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/csv"/>
                <data android:mimeType="text/comma-separated-values"/>
                <data android:mimeType="application/csv"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- **알림 리시버가 둘이다.** 플러그인은 v16부터 receiver를 하나도 선언하지
             않는다(권한 둘만 병합) — 앱이 직접 적어야 한다.

             ⚠️ 아래 `ScheduledNotificationReceiver`가 없으면 **재부팅과 무관하게
                예약 알림이 한 건도 발화하지 않는다.** 이름만 보면 부팅용으로 읽혀
                빠뜨리기 쉽다. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />

        <!-- 부팅/앱 교체 후 재예약. 액션 넷은 플러그인의
             `ScheduledNotificationBootReceiver`가 받는 목록과 1:1이다. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
