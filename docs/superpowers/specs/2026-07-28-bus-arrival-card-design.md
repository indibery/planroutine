# 버스 도착 카드 — 설계

## 개요

오늘 탭 최상단에 **출퇴근 버스 실시간 도착시간 카드**를 얹는다. 데이터는 TAGO(국가대중교통정보센터)
공공데이터이고, 인증키를 숨기기 위해 **Cloudflare Workers 무상태 프록시**를 경유한다.

정류장은 **출발지·도착지 2개 슬롯**만 갖고, 설정 탭에서 언제든 교체한다. 카드는 **기본 OFF**이고
켜지 않은 사용자의 화면은 1픽셀도 바뀌지 않는다.

기획 원본: 옵시디언 `60 프로젝트/플랜루틴(PlanRoutine)/공직플랜 버스 도착 기능 기획.md`

## 배경 — 왜 옵트인인가

이 앱의 목적축은 "매년 반복되는 교사 업무 사이클을 작년 데이터 기반으로 세팅"이다. 오늘 탭은
그 축의 끝단으로, 업무를 체크해 도장을 찍고 결산 링을 채우는 화면이다. **버스 도착은 그 축 밖의
정보**라, 상시 노출하면 화면의 주제가 둘이 된다.

그리고 이 앱은 화면 높이를 예산처럼 다룬다. 진행도 텍스트(`149 / 149 · 100% 완료`)를 지운 이유,
필터 칩을 접은 이유, 중요 배지를 인라인 ★로 바꾼 이유가 전부 픽셀 지출이었다. 카드는 약 80px을
쓴다 — 이 앱에서 작지 않은 지출이다.

그래서 **`설정 > 버스 도착 > 표시` 스위치 기본 OFF**로 두고, 켠 사람만 지출한다. 도장 모양 설정·
`SlideHintBar` 영구 닫기와 같은 계열의 결정이다.

## 기획 원본에서 뒤집은 것 3가지

원본 기획은 Laravel + MySQL + Redis + Sanctum 스택을 전제했다. 확인 결과 **공직플랜에는 백엔드가
없다**(`pubspec.yaml`은 sqflite 로컬 전용, `i_code/`에 Laravel 프로젝트 없음, 볼트의 Laravel 코드는
종료된 민원시스템). "기존 방식 재사용"할 대상이 존재하지 않는다.

### 1. 서버 DB + 로그인 → 무상태 프록시 + 로컬 저장

원본은 `출발/도착 정류장 저장 → 서버 DB (기기 바꿔도 유지)`를 골랐다. 이 한 줄이 회원가입·로그인·
Sanctum 토큰·계정 삭제 기능(App Store 심사 필수)을 전부 끌고 온다. **지금 공직플랜은 로그인이
없는 앱**이다.

게다가 일관성이 안 맞는다 — 기기를 바꾸면 일정·이벤트가 **이미 전부 사라진다**(sqflite 로컬 전용).
정류장 2개만 클라우드에 두는 것은 앱의 나머지와 어긋난다.

정류장을 `shared_preferences`에 두면 서버가 사용자를 모르게 되고, 인증·DB·개인정보·계정 삭제가
동시에 사라진다. **대가**: 기기 교체 시 정류장 2개를 다시 등록해야 한다(수용).

### 2. 두 정류장 항상 표시 → 지금 방향만 + 토글

원본 스케치는 출근·퇴근 정류장을 항상 같이 보여준다. 그러나 **아침 7시 32분에 학교앞 정류장의
실시간 도착시간은 쓸모가 없다.** 실시간 정보의 유효기간은 몇 분뿐이고 퇴근은 8시간 뒤다.

| | 둘 다 표시 | 지금 방향만 |
|---|---|---|
| 오늘 탭 화면 지출 | ~140px | **~80px** |
| TAGO 점유 정류장 | 2개 | **1개** |
| 한도상 동시 사용자 | 약 500명 | **약 1,000명** |

한도 계산 근거(§2의 TTL 60초 기준): `TAGO 호출 = 고유 정류장 수 × (관측시간 / TTL)`.
1인이 한 번에 5분 관측 × 출퇴근 왕복 2회면 정류장 1개당 10 호출/일. 개발계정 10,000/일 ÷ 10 ≈
1,000명. 정류장을 2개 점유하면 절반이 된다. (운영계정 승격 시 10배.)

쓸모없는 정보를 지우면 화면 예산과 호출 예산이 **동시에** 절반이 된다. 지난 항목을 롤링 7일로
자르고 기본 접은 것과 같은 규율이다.

그 결과 원본 6번의 ⭐ 항목 "시간대별 출근/퇴근 자동 전환"의 의미가 바뀐다 — 순서를 뒤바꾸는 게
아니라 **어느 정류장을 폴링할지 고르는 스위치**가 된다. 로직은 더 단순하고 효과는 더 크다.

### 3. 신호등 색 → 신호등 **모양** 3색

원본은 `3분↓ 빨강 / 3~7분 노랑 / 그 이상 초록`이다. 색만 쓰면 이 팔레트와 충돌한다 —
노랑은 골드와 물리적으로 구별되지 않고, 현재 색 규칙은 이미 포화 상태다
(`골드=오늘·중요` / `붉은색=공휴일·일요일` / `파랑=토요일·이벤트`).

해법은 색을 줄이는 것이 아니라 **관용 형태를 주는 것**이다. 신호등 모양 안에 놓인 빨강·노랑·초록은
"공휴일 빨강"이나 "중요 골드"로 읽히지 않는다. 형태가 색의 문법을 지정한다.

부수 효과가 본질적이다 — **어느 칸이 켜졌는가**라는 비색상 단서가 생긴다. CLAUDE.md가 ★를
남긴 논리("남은 신호 셋이 전부 색이라 형태가 마지막 비색상 단서다")와 같고, 색맹 사용자에게도
작동한다.

## 1. 카드 UI

가로 3구. 행 높이를 쓰지 않고, 한국 차량 신호등 순서(좌→우 빨강·노랑·초록)와 일치하며,
왼쪽=임박 / 오른쪽=여유가 위치로 읽힌다.

```
┌───────────────────────────────────┐
│ 🏠→🏫 출근 · 시청앞      07:32 기준 │
│   (●○○)  720번     2분             │
│   (○●○)  150번     5분             │
│   (○○●)  15번     11분             │
│                    [ 퇴근 보기 ⌄ ] │
└───────────────────────────────────┘
```

- **꺼진 칸은 지우지 않고 어둡게 남긴다.** 안 남기면 점 하나가 색만 바뀌는 것이 되어 위치 단서가
  사라진다.
- 세로 3구는 행 높이를 3배 써서 80px 예산에 맞지 않는다.
- `07:32 기준`은 `generatedAt` 노출이다 (§3 참조).
- 방향 토글은 **수동 우선**이다. 누르면 그 화면 수명 동안 유지되고 저장하지 않는다 — 다시 열면
  다시 시각 기준으로 돌아간다. 필터 접힘 상태와 같은 수명 규칙이다.

### 자동 전환 기준

**정오 12:00 고정.** 이전은 출근(집→학교), 이후는 퇴근(학교→집). 교사 일과가 출근 7~8시 /
퇴근 16~17시로 뚜렷해 경계가 애매한 구간이 실질적으로 없다. 설정 항목을 늘리지 않고, 어긋나는
날(야간 자율·조기 퇴재)은 토글이 탈출구다.

### 색 토큰

`AppColors`에 4개를 **추가**한다(기존 값 변경 없음). 다크·라이트 두 `_Palette`에 모두 넣는다.

| 토큰 | 다크 | 라이트 | 용도 |
|---|---|---|---|
| `busSignalNear` | `#EF5F52` | `#CF3A2A` | 3분 미만 |
| `busSignalSoon` | `#F2B23C` | `#C98A0E` | 3~7분 |
| `busSignalFar` | `#5FC98A` | `#1E9E63` | 7분 초과 |
| `busSignalOff` | `#33F0EAD9` | `#D4DBE6` | 꺼진 칸 |

이름에 `bus`를 박는 이유가 있다. 이 색들은 **신호등 형태 안에서만** 유효하다 — 다른 곳에서
`busSignalSoon`을 강조색으로 갖다 쓰면 위에서 회피한 팔레트 붕괴가 그때 일어난다. 이름이 오용을
막는다.

라이트의 노랑은 흰 배경 대비가 없어 딥 앰버(`#C98A0E`)로 잡는다. 이 값은 라이트 `gold`(`#9A7415`)와
색상이 인접하다 — 신호등 형태가 문맥을 주므로 수용하되, **실기기에서 확인할 항목**으로 남긴다.

## 2. 아키텍처

```
[Flutter: features/bus/]
  shared_preferences: 정류장 2슬롯 + 도시코드 + ON/OFF
  30초 폴링 (오늘 탭이 살아있고 앱이 포그라운드일 때만)
      │  https, 인증키 없음, 사용자 식별 없음
      ▼
[Cloudflare Worker]  무상태
  GET /arrival?cityCode=&nodeId=   도착정보 (KV TTL 60초)
  GET /stops?cityCode=&name=       정류장 이름 검색 (KV 6시간)
  GET /cities                      도시코드 목록 (KV 24시간)
      │  serviceKey 주입 — 키가 존재하는 유일한 곳
      ▼
[TAGO 1613000]  ArvlInfoInqireService / BusSttnInfoInqireService
```

### 왜 서버가 필요한가 — 한도가 아니라 고칠 수 있는 속도

산수로는 앱 직접 호출도 약 500명까지 버틴다(정류장 1개 × 30초 폴링 × 5분 × 왕복 2회 = 20 호출/일/명,
서버 캐시가 없으니 폴링 주기가 곧 호출 주기다). 즉 **한도는 서버를 정당화하지 못한다.** 진짜 차이는
**App Store에 배포된 앱에서는 어떤 값도 즉시 바꿀 수 없다**는 점이다.

| 상황 | 앱에 키 내장 | 프록시 |
|---|---|---|
| 키 탈취 | 재발급 → 앱 수정 → **심사 1~3일**, 그동안 기능 정지 | 환경변수 교체 30초 |
| 한도 초과 임박 | 알 방법 없음, 사용자 제보로 발견 | 한 곳에 보임 → TTL 즉시 완화 |
| TAGO 응답 포맷 변경 | 앱 수정 + 심사, 구버전은 계속 깨짐 | `normalize()` 한 곳, 전 버전 즉시 복구 |
| 사용자 급증 | 앱 재심사 없이는 대응 불가 | TTL 조절 |

캐시 TTL은 성능 장치가 아니라 **호출량의 상한선**이다. 그 손잡이가 서버에 있으면 내가 돌리고,
앱에 있으면 심사 뒤에 잠긴다.

부수 이득: TAGO 엔드포인트가 `http://`라 앱이 직접 부르면 iOS ATS 예외를 `Info.plist`에 넣어야
하고 심사에서 소명 대상이 된다. 프록시를 쓰면 앱↔프록시는 https, 프록시↔TAGO는 서버끼리라
질문 자체가 사라진다. (TAGO의 https 지원 여부는 Phase 0에서 실측한다.)

### 캐시 — 원본의 "TTL 15~20초"는 낼 수 없다

**Cloudflare KV의 `expirationTtl` 최소값이 60초**다.

| 방법 | TTL | 문제 |
|---|---|---|
| KV | 최소 60초 | 최대 60초 묵은 값 |
| Cache API (`caches.default`) | 자유 | workers.dev에서 동작 불확실, 커스텀 도메인 필요 가능 |
| isolate 내 `Map` | 자유 | isolate마다 별개라 공유 불완전, 대신 공짜 |

**KV 60초로 시작한다.** TAGO 원본 자체가 30초~1분 주기로 갱신되므로 실질 오차가 크게 늘지 않고,
아래 경과 보정이 대가를 거의 지운다. 체감 문제가 나오면 isolate `Map`을 1차 캐시로 얹어 20초로
조인다(코드 5줄, 되돌리기 쉬움).

### 경과 보정이 TTL 60초의 대가를 지운다

`generatedAt`이 있으니 앱에서 묵은 만큼 차감한다.

```dart
// bus_card_view.dart
final elapsed = now.difference(generatedAt).inSeconds;
final displayMin = ((arrival.arrMin * 60 - elapsed) / 60).ceil();
```

서버가 "4분"이라고 준 값이 50초 묵었으면 화면에는 **3분**으로 나간다. 폴링 30초 + TTL 60초에서
최대 90초까지 묵을 수 있지만 보정이 그것을 상쇄한다. 원본이 TTL을 20초까지 조이려 한 이유가
이 방법으로 거의 사라진다.

## 3. 실패 계약 — 신뢰의 급소

원본 6번이 "명확한 빈 상태 문구 — 신뢰에 직결"이라고 짚은 것을 계약으로 못박는다.
**네 상태가 화면에서 서로 구분돼야 한다.**

| 상황 | 서버 응답 | 카드 표시 |
|---|---|---|
| 정상 | `200 {buses:[...], generatedAt}` | 목록 + "07:32 기준" |
| 도착정보 없음 (막차 후 등) | `200 {buses:[]}` | "오늘 운행이 끝났어요" |
| TAGO 장애 + 캐시에 옛 값 | `200 {buses:[...], stale:true}` | 목록 + "07:30 기준 (갱신 실패)" |
| TAGO 장애 + 캐시 비었음 | `502 {error}` | "지금 정보를 못 받았어요 · 다시 시도" |

`buses: []`와 `502`를 같은 문구로 뭉개면 사용자가 "막차 끝남"과 "앱 고장"을 구별할 수 없다.
버스 앱에서 이 둘의 차이는 **정류장에서 기다릴지 택시를 부를지**를 가른다.

### 응답 정규화 (서버가 계산, 앱은 표시)

- `arrtime`(초) → `arrMin`: `ceil(sec / 60)`, 0이면 "곧 도착"
- `buses`는 `arrtime` 오름차순 정렬
- `generatedAt`: 실제 TAGO 호출 시각(캐시 히트면 캐시된 값 그대로)
- `vehicletp` → `lowFloor` bool

## 4. 정류장 등록 — 방향이 조용히 틀리는 것을 막는다

**TAGO `nodeId`는 방향별로 별개다.** "시청앞" 한 곳이 아니라 상행 정류장 / 하행 정류장이 각각
다른 ID를 갖는다. 길 건너 마주보는 두 정류장이 같은 이름이다. 잘못 고르면 화면에는 버스가 정상적으로
뜨지만 **전부 반대 방향**이다 — 사용자는 앱이 고장 났다고 생각하지 않고 자기가 늦었다고 생각한다.

### 저장 직전 확인 시트

정류장을 탭하면 바로 저장하지 않고, 그 정류장에 실제로 오는 버스를 한 번 조회해 보여준다.

```
┌─────────────────────────────┐
│  이 정류장이 맞나요?           │
│  시청앞 (12-345)             │
│                             │
│  지금 이 정류장에 오는 버스     │
│   720번   4분 후             │
│   150번   11분 후            │
│   15번    운행 종료           │
│                             │
│    [ 아니에요 ]  [ 맞아요 ]   │
└─────────────────────────────┘
```

방향은 좌표나 이름으로 사람이 판별할 수 없지만 **자기가 타는 버스 번호는 안다.** 노선 번호를
보여주면 사용자가 스스로 맞는 쪽을 고른다. 호출 1회가 늘지만 `/arrival` 캐시를 그대로 재사용한다.

### 검색 방식 — 이름 검색만

`getSttnNoList`(이름 검색)는 `cityCode`가 필수라 "도시 선택 → 정류장명" 2단이 된다.
`getCrdntPrxmtSttnList`(좌표 근접)는 위·경도만 받아 도시 선택을 우회하지만, **GPS는 교체
시나리오에서 못 쓴다** — 3월에 학교를 옮기면 새 학교 정류장을 집에서 등록하고, 이사하면 새 집
정류장을 이사 전 집에서 등록한다. 대상 정류장 근처에 없는 것이 교체의 기본값이다.

이름 검색만 넣는다. 새 의존성 0, 새 권한 0, 위치 권한 소명 0. 도시는 마지막 선택을 기억해 매번
고르지 않는다.

## 5. 스크래핑 대응 (무상태의 대가)

로그인을 없앴으니 엔드포인트는 누구나 부를 수 있는 공개 URL이다. 남이 전국 정류장을 순회하면
우리 TAGO 한도가 소진된다. 원본(Sanctum 인증)에는 없던 문제이고, 로그인을 뺀 대가다.

1차 출하는 **IP당 rate limit**과 **일일 총량 상한**만 넣는다. 정상 사용자는 60초에 1회 미스인데
스크래퍼는 훨씬 빠르다.

- IP당: 60초에 10회 (정상 사용자 최대치의 5배 — 한 IP를 여러 명이 공유하는 학교 와이파이 감안)
- 일일 TAGO 총량: **8,000회** (개발계정 10,000의 80%). 초과 시 캐시된 값만 반환하고 미스는
  `stale` 없는 `502`로 답한다 — 최악의 경우에도 TAGO 계정이 죽지 않는다.

두 값 모두 Worker 환경변수로 둔다. 상한을 코드에 박으면 조절하려고 배포해야 하는데, 그건 이 설계가
프록시를 택한 이유(§2 "고칠 수 있는 속도")와 모순이다.

앱 내장 공유 시크릿은 **실제로 긁히는 것이 관측되면** 붙인다. 미리 넣으면 회전 절차까지 만들어야
하는데 아직 필요를 모른다. TAGO 키와 달리 우리 시크릿은 심사 대기 없이 회전할 수 있다는 점이
이 대응의 핵심이므로, 붙일 때 회전 절차를 함께 만든다.

## 6. 파일 구조

새 의존성 0개다. `http`는 Google 연동용으로, `shared_preferences`는 알림·테마 설정용으로 이미 있다.

```
lib/features/bus/
├── data/bus_api_client.dart        # 프록시 호출 + 4상태 실패 계약
├── domain/
│   ├── bus_stop.dart               # freezed: nodeId, nodeNm, nodeNo, cityCode
│   ├── bus_arrival.dart            # freezed: routeNo, arrMin, prevCnt, lowFloor
│   ├── bus_slots.dart              # freezed: 출발지/도착지 2슬롯 + cityCode + enabled
│   ├── commute_direction.dart      # enum toWork/toHome + 정오 기준 판정
│   └── bus_card_view.dart          # 순수 함수 — 카드가 그릴 상태 전부
└── presentation/
    ├── providers/bus_providers.dart
    ├── widgets/
    │   ├── bus_arrival_card.dart    # 카드 껍데기 + 헤더 + 방향 토글
    │   ├── bus_arrival_row.dart     # 신호등 + 노선번호 + 분
    │   ├── bus_signal_lamp.dart     # 가로 3구 신호등 (CustomPainter 또는 Row)
    │   └── bus_empty_state.dart     # 실패 계약 4상태 문구
    └── screens/bus_stop_search_screen.dart

lib/features/settings/presentation/widgets/bus_settings_tiles.dart
lib/core/constants/strings/bus_strings.dart   # app_strings.dart가 barrel export
```

`bus_card_view.dart`의 순수 함수는 이 앱의 기존 패턴이다 — `buildTodayView`·`computeNotifications`·
`buildFilterSummary`와 같은 자리다. DB도 플랫폼도 모르니 색상 경계·경과 보정·빈 상태를 전부
유닛 테스트로 고정한다.

### 설정 탭

`SettingsSection(title: '버스 도착')`:
- `표시` 스위치 (기본 OFF)
- ON일 때만 슬롯 2줄: `출발지 · 시청앞`, `도착지 · 학교앞` → 탭하면 `/bus/stops` push

### 오늘 탭 배선 — `buildTodayView`는 건드리지 않는다

카드는 `today_body.dart` 최상단에 조건부로 들어가지만 `TodayView`에는 섞지 않는다.
`buildTodayView`는 업무 완료·진행도 계산 함수이고 버스는 그 도메인이 아니다. 섞으면 순수 함수
하나가 두 주제를 갖고 기존 테스트가 흔들린다. 카드는 별 provider를 watch하는 형제 위젯이다.
**기존 480개 테스트는 무손상이다.**

### CLAUDE.md의 push 함정이 여기 정확히 걸린다

> ⚠️ `push`는 예외다. `push`로 덮인 화면은 offstage로 마운트가 유지돼 autoDispose가 걸리지 않는다.
> **오늘 탭에 push 라우트를 붙이면** 그 화면이 리비전을 올리지 않는 한 pop 후 목록이 stale이 된다.

카드의 빈 상태에서 `/bus/stops`로 push하면 이 조건이 성립한다. 정류장을 바꿔 돌아왔는데 카드가
옛 정류장을 계속 폴링하는 버그가 된다.

대응: 저장 시 `shared_preferences` 쓰기와 **동시에 `busSlotsProvider` state를 갱신**하고, 카드는
그 provider를 watch해 슬롯이 바뀌면 폴링을 새 `nodeId`로 재시작한다. 위젯 테스트로 고정한다.

### 폴링 수명

- 주기 30초. 오늘 탭이 살아 있고 앱이 포그라운드일 때만.
- 백그라운드 진입 시 정지(`AppLifecycleListener`), 복귀 시 즉시 1회 조회 후 재개.
- 카드 provider는 `autoDispose` — `todayViewProvider`와 같은 이유다(이 앱은 `ShellRoute`라
  탭을 옮기면 화면이 dispose된다).

## 7. 개인정보

무상태라 서버가 사용자를 모른다. rate limit 때문에 Cloudflare 로그에 IP가 잠시 남으므로 **로그
보존을 최소로 설정**한다. App Store 개인정보 설문의 "데이터 수집 없음"은 유지된다.

단 이 기능은 앱의 **첫 네트워크 사용**이다. `docs/privacy_policy.md`에 "버스 도착 조회 시
정류장 ID가 프록시 서버로 전송되며, 서버는 사용자를 식별하지 않는다"는 한 문단을 추가한다.

## 8. 테스트

| 종류 | 내용 |
|---|---|
| 유닛 | `buildBusCardView` — 경과 보정, 3분/7분 경계, 0분(곧 도착), 4상태 |
| 유닛 | `commute_direction` — 정오 경계(11:59 / 12:00 / 12:01) |
| 유닛 | `bus_api_client` — `MockClient`로 정규화 + 실패 계약 4종 |
| 위젯 | 카드 렌더 — 신호등 3구 중 켜진 칸, 꺼진 칸 유지, 방향 토글 |
| 위젯 | 빈 상태 4종이 서로 다른 문구를 낸다 |
| 위젯 | 설정 타일 — OFF→ON 시 슬롯 2줄 노출 |
| **가드** | 스위치 OFF면 오늘 탭에 `BusArrivalCard`가 `findsNothing` (화면 불변 약속) |
| **가드** | ON + 슬롯 미설정이면 등록 유도가 뜬다 (무한 로딩 금지) |
| **가드** | 슬롯 교체 후 pop하면 카드가 새 `nodeId`를 폴링한다 (push 함정) |
| **가드** | 신호등 3구가 지정 폭 안에 들어간다 (도장 44px 가드와 같은 계열) |
| Worker | vitest — 캐시 히트/미스, TAGO 실패 시 stale, rate limit, 총량 상한 |

위젯 테스트에서 실제 DB/네트워크 I/O는 `tester.runAsync()` 안에서 한다.

## 9. 단계

| Phase | 내용 | 완료 신호 |
|---|---|---|
| **0** | curl 실측 — 경기도 도시코드, 도착정보 호출, **https 지원 여부**, 검색 API 파라미터 스펙, Encoding/Decoding 키 구분 | 확인된 사실 목록 |
| 1 | Worker: 3엔드포인트 + KV 캐시 + 실패 계약 + rate limit | vitest green, 배포 URL |
| 2 | 앱: domain + api client + 순수 함수 | 유닛 green, 화면 없음 |
| 3 | 설정 탭 슬롯 + 검색 화면 + 확인 시트 | 위젯 green |
| 4 | 오늘 탭 카드 + 신호등 + 방향 토글 + 폴링/라이프사이클 | 가드 green |
| 5 | 시뮬레이터 검증 → TestFlight 실기기 | 실사용 확인 |

**Phase 0이 나머지 전부의 전제다.** 원본 8번 함정 3번(`cityCode` 없으면 호출 불가)과 https 여부가
여기서 결정되고, 결과에 따라 Phase 1의 코드가 달라진다.

Phase 2가 **화면 없이 끝나는 것이 의도**다. 순수 함수 + API 클라이언트를 먼저 세우면 색상 경계·
경과 보정·실패 4상태를 위젯 없이 전부 테스트할 수 있고, Phase 3~4는 그 결과를 그리기만 한다.
이 앱이 `buildTodayView`·`computeNotifications`·`buildFilterSummary`를 모두 이 순서로 만든 이유다.

## 10. 범위에서 뺀 것

원본 6번의 "넣지 말 것"을 그대로 유지한다.

- **길찾기·경로검색** — ODsay 영역, 무거움. 네이버지도가 할 일.
- **즐겨찾기 무한 등록** — 2슬롯으로 충분.
- **실시간 지도 버스 아이콘** — 호출량·복잡도 큼. "분"이면 충분.
- **푸시 알림** — 백그라운드 폴링이 필요해 서버·배터리 부담. 무상태 프록시로는 애초에 불가
  (서버가 사용자를 모른다). 1단계 제외.
- **OS 홈화면 위젯** — 네이티브 WidgetKit 필요 + OS가 실시간 갱신을 극도로 제한해 버스 도착과
  상성이 나쁘다.
- **일정 장소 자동 버스 추천 / 출발시간 역산** — 원본 2단계. 길찾기 의존.

원본 6번의 "여력되면" 항목 중 **저상버스 표시**(`vehicletp` → 아이콘 하나)는 Phase 4에서 여력이
있으면 넣고, 없으면 뺀다. 신호등 3구가 이미 행의 좌측을 쓰므로 아이콘 위치는 노선번호 뒤가 된다.
